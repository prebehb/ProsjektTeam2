const spiller = {
    X : 100,
    y: 300,
    width: 40,
    height: 40,
    color: #0099ff,
    vy: 0,
    gravity: 1,
    jumpPower: - 14,
    isOnGround: true,
};
const bakke = 80; // høyde på bakken inne i spillet

function spill(){ //Selve spillet
    if (spiller.y + spiller.height >= canvas.height - bakke){
        
    }

}